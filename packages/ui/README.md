# @lcllcllcl/ui

一个基于 React 和 styled-components 的现代化 UI 组件库。

## ✨ 特性

- 🎨 精美的设计和流畅的动画
- 📦 开箱即用的高质量 React 组件
- 🛡 使用 TypeScript 编写，提供完整的类型定义
- 🎯 支持 ESM 和 IIFE 模块格式
- 💪 基于 styled-components 的样式方案
- ✅ 完善的单元测试覆盖

## 📦 安装

```bash
# 使用 npm
npm install @lcllcllcl/ui

# 使用 pnpm
pnpm add @lcllcllcl/ui

# 使用 yarn
yarn add @lcllcllcl/ui
```

## 🔨 使用

```jsx
import { Button } from '@lcllcllcl/ui';

function App() {
  return (
    <Button type="primary" onClick={() => console.log('clicked')}>
      点击我
    </Button>
  );
}
```

## 📚 组件

目前支持的组件：

- Button - 按钮组件
- 更多组件持续更新中...

## 🔧 开发

```bash
# 安装依赖
pnpm install

# 启动开发模式
pnpm dev

# 构建
pnpm build

# 运行测试
pnpm test
```

## 📄 许可证

ISC

## 👤 作者

Heyi
