# 登录认证时 (npm login)：使用的是全局 registry 配置，而不是 publishConfig
# 下载依赖时 (npm install)：使用的是全局 registry 配置
# 发布包时 (npm publish)：才会使用 package.json中的publishConfig.registry

registry=https://registry.npmjs.org/

# 访问令牌配置（用于发布包）
# 请将下面的 YOUR_NPM_TOKEN 替换为你在 npmjs.com 生成的访问令牌
//registry.npmjs.org/:_authToken=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